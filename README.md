# springboot-azure-app-service
Sample Hello world springboot application to deploy on Azure App Service.

## Java version
This project now targets Java 21.
Use JDK 21 for local development, CI builds, and Azure runtime configuration.

## Build
Run `mvn clean verify` with JDK 21.
