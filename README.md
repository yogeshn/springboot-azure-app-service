# springboot-azure-app-service
Sample Hello world springboot application to deploy on Azure App Service.

Requires Java 21 (JDK 21) for local development, builds, and runtime.

Use JDK 21 when running Maven locally or in CI.
