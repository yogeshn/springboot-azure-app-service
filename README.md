# springboot-azure-app-service
Sample Hello world springboot application to deploy on Azure App Service.

Requires JDK 21 for local development, builds, testing, and deployment. The project is configured for Spring Boot 3.2.x and Java 21.
