# springboot-azure-app-service
Sample Hello world Spring Boot application to deploy on Azure App Service.

## Prerequisites

- JDK 21 (or later)
- Maven 3.x

This project was previously targeting Java 8 and has been updated to use Java 21 across the build (Maven),
CI (GitHub Actions and Azure Pipelines), and Azure App Service runtime configuration.
