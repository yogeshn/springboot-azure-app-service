<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.1.0</version> <!-- Updated to Spring Boot 3.1.0 for Java 17 compatibility -->
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.amock</groupId>
    <artifactId>helloazure</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>helloazure</name>
    <description>Hello Azure Spring Boot Application</description>
    <properties>
        <java.version>17</java.version> <!-- Explicitly set to Java 17 for compliance -->
        <maven.compiler.source>17</maven.compiler.source> <!-- Compiler source version mapping: Java 17 -->
        <maven.compiler.target>17</maven.compiler.target> <!-- Compiler target version mapping: Java 17 -->
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Additional dependencies preserved from existing project structure; no changes needed for Java 17 -->
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <!-- Maven Compiler Plugin configuration for Java 17 compliance -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version> <!-- Version compatible with Java 17 -->
                <configuration>
                    <source>17</source> <!-- Ensures source compatibility with Java 17 -->
                    <target>17</target> <!-- Ensures target compatibility with Java 17 -->
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>