# Application Configuration
spring.application.name=springboot-azure-app-service
server.port=9093

# Java Version (Assumed based on Spring Boot compatibility)
# java.version=17

# Build Tool Detection
# Maven: Check for pom.xml in project root
# Gradle: Check for build.gradle or build.gradle.kts in project root

# Project Status Assessment
# - Zero lines of production code detected
# - Application properties file exists with minimal configuration
# - Project appears to be in initial setup phase

# Assumptions for Upgrade Scope:
# 1. Target Java Version: 17 or 21 (LTS versions)
# 2. Spring Boot Version: 3.x (requires Java 17+)
# 3. Build Tool: Maven or Gradle (to be confirmed from build files)
# 4. Dependencies: Spring Boot Starter Web (minimal for Azure App Service)
# 5. Project Structure: Standard Maven/Gradle layout expected
#    - src/main/java (empty or missing)
#    - src/main/resources (exists with this file)
#    - src/test/java (to be verified)

# Configuration for Azure App Service
# azure.app-service.enabled=true

# Logging Configuration (for diagnostics)
logging.level.root=INFO
logging.level.org.springframework.web=DEBUG

# Actuator endpoints (recommended for cloud deployment)
# management.endpoints.web.exposure.include=health,info,metrics
# management.endpoint.health.show-details=always